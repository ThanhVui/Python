inp = input('Europe floor? ')
# input() runs first, then it stores the value into variable inp
usf = int(inp) + 1
print("US floor", usf)
# This code takes a floor number in Europe and converts it to the US floor number.
# It adds 1 to the input number and prints the result.